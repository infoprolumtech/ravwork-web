import { styled } from "@mui/material";

export const WarningLine = styled("div")({
    backgroundColor: "#F79009", // Green color for the line
    width: "4px",
    height: 60,
    position: "absolute",
    top: 20,
    left: 0,
    borderRadius: "100px",
});